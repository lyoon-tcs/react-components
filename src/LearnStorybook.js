// dependencies
import React, { Fragment } from 'react';
// local files

const LearnStorybook = () => {
  return <Fragment>Learn Storybook!!!</Fragment>;
};

export default LearnStorybook;

// import React from 'react';
// import PropTypes from 'prop-types';

// export default function Button({ label, backgroundColor, onClick }) {
//   return (
//     <button onClick={onClick} style={backgroundColor && { backgroundColor }}>
//       {label}
//     </button>
//   );
// }

// Button.propTypes = {
//   backgroundColor: PropTypes.string,
//   label: PropTypes.string.isRequired,
//   onClick: PropTypes.func
// };

// Button.defaultProps = {
//   backgroundColor: null,
//   onClick: undefined
// };
